name: "Lint"

on:
  pull_request:

jobs:
  phpstan:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Composer install
        run: composer install
      - name: lint
        run: composer lint
